src=$(wildcard *.cpp)
objs=$(patsubst %.cpp,%.o,$(src))
target=computerRoomAppointment
LIBS=-lstdc++ 

$(target):$(objs)
	g++ -g $@ $^ $(LIBS)

%.o:%.cpp
	g++ -std=c++11 -c $<

.PHONY:clean
clean:
	rm -f $(target) $(objs)
